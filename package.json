{
  "name": "sip-caller",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --quiet src/index.ts",
    "dev:consumer": "ts-node-dev --respawn --transpile-only --quiet src/consumer.ts"
  },
  "devDependencies": {
    "@types/websocket": "^1.0.8",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "joi": "^17.11.0",
    "websocket": "^1.0.34"
  }
}
